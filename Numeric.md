

# Численный алгоритм проверки порождения $W_n$ до степени $d$

## 1. Формализация задачи

**Дано:** Генераторы $G_1, \ldots, G_m \in W_n$, целые числа $n, d$.

**Вопрос:** Верно ли, что $\operatorname{Lie}(G_1, \ldots, G_m) \supseteq W_n^{(\leqslant d)} = \bigoplus_{k=-1}^{d} W_n^{(k)}$?

Здесь $W_n^{(k)} = \operatorname{span}\{z^\alpha \partial_{z_i} : |\alpha| = k+1,\; 1 \leqslant i \leqslant n\}$ — однородная компонента степени $k$.

---

## 2. Математический фундамент

### 2.1. Градуировка как ключевой инструмент

Положим $\deg(z_i) = 1$, $\deg(\partial_{z_i}) = -1$. Тогда $\deg(z^\alpha \partial_{z_i}) = |\alpha| - 1$, и скобка Ли **складывает** степени:
$$[W_n^{(p)}, W_n^{(q)}] \subseteq W_n^{(p+q)}$$

Это означает, что порождённая подалгебра $L = \operatorname{Lie}(G_1, \ldots, G_m)$ наследует градуировку:
$$L = \bigoplus_{k \geqslant -1} L^{(k)}, \quad \text{где } L^{(k)} = L \cap W_n^{(k)}$$

### 2.2. Рекуррентное соотношение для $L^{(r)}$

**Теорема (замкнутость по степеням).** Компоненты $L^{(r)}$ удовлетворяют:

$$L^{(r)} = \operatorname{span}\!\Bigl(\{G_i^{(r)}\}_{i=1}^m\Bigr) + \sum_{\substack{p+q=r \\ p,q \geqslant -1}} [L^{(p)}, L^{(q)}]$$

где $G_i^{(r)}$ — однородная компонента степени $r$ генератора $G_i$.

**Доказательство:** Любой элемент $L^{(r)}$ — линейная комбинация итерированных скобок однородных компонент генераторов. Степень итерированной скобки $[\ldots[[G_{i_1}^{(p_1)}, G_{i_2}^{(p_2)}], G_{i_3}^{(p_3)}], \ldots]$ равна $\sum p_j$. Раскладывая по внутренним узлам дерева скобок и применяя индукцию по глубине, получаем указанное соотношение. $\square$

### 2.3. Размерности

$$\dim W_n^{(k)} = n \binom{k+n}{n-1}, \quad k \geqslant -1$$

$$\dim W_n^{(\leqslant d)} = n \binom{d+n+1}{n}$$

| $n$ | $d$ | $\dim W_n^{(\leqslant d)}$ |
|-----|-----|---------------------------|
| 2 | 5 | 56 |
| 2 | 10 | 156 |
| 3 | 5 | 168 |
| 3 | 10 | 1092 |
| 5 | 5 | 2310 |
| 5 | 10 | 48048 |

### 2.4. Структурные константы: явная формула

Для базисных элементов $e_{\alpha,i} = z^\alpha \partial_{z_i}$ и $e_{\beta,j} = z^\beta \partial_{z_j}$:

$$\boxed{[z^\alpha \partial_{z_i},\; z^\beta \partial_{z_j}] = \beta_i \cdot z^{\alpha+\beta-e_i}\partial_{z_j} - \alpha_j \cdot z^{\alpha+\beta-e_j}\partial_{z_i}}$$

**Ключевое свойство разреженности:** каждая скобка пары базисных элементов содержит **не более 2 ненулевых** компонент в целевом базисе. Это делает тензор структурных констант экстремально разреженным.

---

## 3. Алгоритм

### 3.1. Структуры данных

**Индексация мономов.** Для каждой степени $s \geqslant 0$ строим упорядоченный список мультииндексов:
$$\mathcal{M}(s) = \{\alpha \in \mathbb{Z}_{\geqslant 0}^n : |\alpha| = s\}$$
и обратный словарь $\operatorname{idx}_s: \mathcal{M}(s) \to \{0, 1, \ldots, |\mathcal{M}(s)|-1\}$.

**Индексация базиса $W_n^{(k)}$.** Базисный элемент $z^\alpha \partial_{z_i}$ (где $|\alpha| = k+1$) получает индекс:
$$\operatorname{Idx}(\alpha, i) = (i-1) \cdot |\mathcal{M}(k+1)| + \operatorname{idx}_{k+1}(\alpha)$$

**Представление подпространства $L^{(k)}$.** Матрица $B_k \in \mathbb{R}^{r_k \times N_k}$, строки которой образуют базис $L^{(k)}$. Храним в приведённой ступенчатой форме (RREF) или в виде ортонормированного базиса (QR).

### 3.2. Вычисление скобки

Вычисление $[u, v]$ для $u \in W_n^{(p)}$, $v \in W_n^{(q)}$:

```
function BRACKET(u, p, v, q, n):
    r = p + q
    result = zeros(dim W_n^(r))
    for each (α, i) with |α| = p+1 and u[Idx(α,i)] ≠ 0:
        for each (β, j) with |β| = q+1 and v[Idx(β,j)] ≠ 0:
            c = u[Idx(α,i)] * v[Idx(β,j)]
            // Первый член: β_i · z^{α+β-e_i} ∂_{z_j}
            if β[i] > 0:
                γ = α + β - e_i     // мультииндекс, |γ| = r+1
                result[Idx(γ,j)] += c * β[i]
            // Второй член: -α_j · z^{α+β-e_j} ∂_{z_i}
            if α[j] > 0:
                γ = α + β - e_j
                result[Idx(γ,i)] -= c * α[j]
    return result
```

**Сложность одной скобки:** $O(N_p \cdot N_q / n^2)$ операций благодаря разреженности (бо́льшая часть пар $(\alpha, i), (\beta, j)$ даёт тривиальные вклады, если коэффициенты $u, v$ разрежены).

### 3.3. Пакетное вычисление скобок

Для пары степеней $(p, q)$ с $r = p+q$ предвычислим **разреженный тензор структурных констант:**

$$C^{(p,q)} = \{(a, b, c, \lambda) : [e_a^{(p)}, e_b^{(q)}]_c = \lambda \neq 0\}$$

Тогда все скобки строк $B_p$ со строками $B_q$ вычисляются как:

$$W_{(s,t),c} = \sum_{(a,b,c,\lambda) \in C^{(p,q)}} (B_p)_{s,a} \cdot (B_q)_{t,b} \cdot \lambda$$

Через разреженные матрицы $M_c^{(p,q)}$ (для каждого выходного индекса $c$):

$$W_{:,:,c} = B_p \cdot M_c^{(p,q)} \cdot B_q^T$$

где $M_c^{(p,q)} \in \mathbb{R}^{N_p \times N_q}$ — разреженная матрица с не более чем $2N_p$ ненулевыми элементами суммарно по всем $c$.

### 3.4. Основной цикл

```
ALGORITHM LieGenerationCheck(n, d, generators G₁,...,Gₘ):

    D = max(d, max degree of any Gᵢ component)
    
    // ШАГI: Инициализация
    for k = -1 to D:
        B[k] = matrix of degree-k components of G₁,...,Gₘ
        B[k] = ReduceToBasis(B[k])   // RREF или QR
    
    // ШАГ II: Предвычисление структурных констант
    for each pair (p, q) with -1 ≤ p ≤ q, p+q ≤ D:
        Precompute sparse tensor C^(p,q)
    
    // ШАГ III: Итеративное замыкание
    changed = true
    while changed:
        changed = false
        for r = -1 to D:
            new_rows = empty matrix
            for p = max(-1, r-D) to min(D, r+1):
                q = r - p
                if q < -1 or q > D or q < p: continue
                if B[p] is empty or B[q] is empty: continue
                
                // Вычислить все скобки [B[p], B[q]]
                brackets = BatchBracket(B[p], p, B[q], q)
                new_rows = stack(new_rows, brackets)
            
            if new_rows is not empty:
                combined = stack(B[r], new_rows)
                B_new = ReduceToBasis(combined)
                if rank(B_new) > rank(B[r]):
                    B[r] = B_new
                    changed = true
    
    // ШАГ IV: Проверка
    for k = -1 to d:
        N_k = n * C(k+n, n-1)
        if rank(B[k]) == N_k:
            report "Degree k: FULL"
        else:
            report "Degree k: rank {rank(B[k])} / {N_k}"
```

---

## 4. Анализ корректности

### 4.1. Завершение

**Утверждение.** Алгоритм завершается за конечное число итераций.

**Доказательство.** На каждой итерации $\operatorname{rank}(B_k)$ не убывает для всех $k$, и ограничен сверху $\dim W_n^{(k)} < \infty$. Если хотя бы один ранг возрос — итерация не последняя. Поскольку рангов конечное число и они ограничены, процесс завершается. $\square$

**Оценка числа итераций:** не более $\sum_{k=-1}^{D} \dim W_n^{(k)} = \dim W_n^{(\leqslant D)}$, но на практике — $O(\text{глубина доказательства})$.

### 4.2. Достаточность усечения

**Теорема.** Пусть $L_D^{(k)}$ — компонента степени $k$ алгебры, порождённой генераторами в усечённом пространстве $W_n^{(\leqslant D)}$. Тогда $L_D^{(k)} \subseteq L_{D+1}^{(k)} \subseteq \ldots$ — неубывающая последовательность, стабилизирующаяся при $D \to \infty$ к $L^{(k)}$.

**Следствие.** Для проверки порождения до степени $d$ достаточно запустить алгоритм с $D = d$, затем увеличивать $D$ до стабилизации.

**Для генераторов Бельдиева:** $D = 4n - 1$ заведомо достаточно, так как доказательство теоремы 3.1 использует только нисходящие цепочки $\operatorname{ad}_U^k(V)$, не выходящие за максимальную степень $V$.

### 4.3 Пример проверки для $n = 2$

Генераторы Бельдиева для $n = 2$:
$$U = \partial_y, \qquad V = y^8 \partial_x + (xy)^4 \partial_y$$

Шаг 1 из доказательства: $\operatorname{ad}_U^5(V) = c \cdot y^3 \partial_x$ (степень 2), далее $\operatorname{ad}_U$: $y^2\partial_x, y\partial_x, \partial_x$ (степени 1, 0, -1).

Проверка: после $\sim 7$ итераций алгоритм должен подтвердить $L^{(k)} = W_2^{(k)}$ для $k \leqslant 7$.

---

## 5. Оптимизации для больших $n$

### 5.1. Инкрементальное обновление

Храним для каждой степени $k$ два множества: $B_k^{\text{old}}$ (из предыдущей итерации) и $B_k^{\text{new}}$ (новые строки). В следующей итерации вычисляем только скобки, где хотя бы один аргумент из $B^{\text{new}}$:

$$\text{новые скобки на степени } r = [B_p^{\text{new}}, B_q^{\text{all}}] \cup [B_p^{\text{all}}, B_q^{\text{new}}]$$

Это даёт **существенное ускорение** на поздних итерациях, когда подпространства растут медленно.

### 5.2. Ортогональная проекция вместо RREF

Поддерживаем $B_k$ как матрицу с ортонормальными строками (QR-декомпозиция). Добавление нового вектора $v$:

$$v_\perp = v - B_k^T (B_k \cdot v)$$

Если $\|v_\perp\| > \varepsilon$, добавляем $v_\perp / \|v_\perp\|$ как новую строку. 

**Сложность добавления одного вектора:** $O(r_k \cdot N_k)$.

### 5.3. Использование представлений $\mathfrak{gl}_n$

Степень $0$ компонента $W_n^{(0)} \cong \mathfrak{gl}_n$ действует на каждую $W_n^{(k)}$ через присоединённое представление. Если $L^{(0)} \supseteq \mathfrak{sl}_n$, то $W_n^{(k)}$ как $\mathfrak{sl}_n$-модуль разлагается в прямую сумму неприводимых:

$$W_n^{(k)} \cong \bigoplus_\lambda V_\lambda$$

и достаточно проверить, что $L^{(k)}$ содержит хотя бы один вектор из каждой неприводимой компоненты — остальные получаются действием $\mathfrak{sl}_n$.

Для $n = 2$: $W_2^{(k)}$ как $\mathfrak{sl}_2$-мо��уль разлагается в $V_k \oplus V_{k+2}$ (представления размерности $k+1$ и $k+3$). Достаточно проверить наличие старшего вектора каждой компоненты.

### 5.4. Послойная обработка с ранним прекращением

Если для степени $k$ уже $\operatorname{rank}(B_k) = \dim W_n^{(k)}$, скобки $[L^{(p)}, L^{(q)}]$ с $p + q = k$ **не нужно вычислять** — они не дадут ничего нового в степени $k$.

Но эти элементы по-прежнему нужно использовать как **аргументы** скобок для других степеней!

---

## 6. Анализ вычислительной сложности

### 6.1. Стоимость одной итерации

Для целевой степени $r$:
- Число пар $(p, q)$: $O(D)$
- Для каждой пары: $r_p \cdot r_q$ скобок, каждая стоимостью $O(N_p + N_q)$ (разреженные структурные константы)
- Обновление $B_r$: $O((r_r + r_p r_q)^2 \cdot N_r)$ (QR)

### 6.2. Суммарная стоимость

$$T = O\!\left(\text{число итераций} \cdot \sum_{r=-1}^{D} \sum_{\substack{p+q=r\\p \leqslant q}} r_p \cdot r_q \cdot N_{p+q}\right)$$

Для задачи 1.5-порождаемости ($m = 2$ генератора), типичные ранги малы на начальных итерациях.

### 6.3. Практические пределы

| $n$ | $d$ | $\dim W_n^{(\leqslant d)}$ | Время (оценка) |
|-----|-----|---------------------------|----------------|
| 2 | 20 | 506 | < 1 сек |
| 3 | 10 | 1092 | ~ 1 сек |
| 5 | 5 | 2310 | ~ 10 сек |
| 5 | 10 | 48048 | ~ минуты |
| 10 | 3 | 14300 | ~ минуты |

---

## 7. Применение к задаче 1.5-порождаемости

Для проверки гипотезы о 1.5-порождаемости фиксированного $D \in W_n$:

1. **Фиксируем $D$** (например, $D = x^5y^5\partial_x$ в $W_2$).
2. **Выбираем случайный $E$** достаточно общего вида (все мономы степени $\leqslant d_E$ с коэффициентами из $\mathbb{Q}$ или случайными).
3. **Запускаем алгоритм** для генераторов $\{D, E\}$.
4. **Интерпретируем:**
   - Если $L^{(k)} = W_n^{(k)}$ для всех $k \leqslant d$ — **гипотеза подтверждается** (численно, для данного $E$).
   - Если застряло — увеличиваем $d_E$ или пробуем другой $E$.
5. **Если для всех** $E$ (достаточно общих) порождение не достигается — это **контрпример**.

**Обоснование подхода «случайного $E$»:** Если для данного $D$ множество «хороших» $E$ (таких, что $\operatorname{Lie}(D, E) = W_n$) непусто и является открытым подмножеством в пространстве генераторов, то случайный $E$ попадёт в него с вероятностью 1.

---

## 8. Корректность для генераторов Бельдиева

Для генераторов $U = \partial_{z_n}$ и $V = z_n^{4n}\partial_{z_{n-1}} + \ldots + (z_n \cdots z_1)^4 \partial_{z_n}$:

- **Максимальная степень $V$:** $4n - 1$ (от компоненты $(z_n \cdots z_1)^4\partial_{z_n}$).
- **Необходимое усечение:** $D = \max(d, 4n-1)$.
- **Число итераций:** $O(n)$ (соответствует $n$ шагам индукции в доказательстве теоремы 3.1).
- **Проверяемый факт:** $\operatorname{rank}(B_k) = \dim W_n^{(k)}$ для всех $k \leqslant d$.

Алгоритм воспроизводит доказательство Бельдиева в числовой форме и позволяет **верифицировать** его для конкретных $n$ и $d$.
